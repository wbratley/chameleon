mail_location = maildir:/srv/mail/chameleon
# No mail_privileged_group is set: mail processes already run as the numeric
# uid/gid 5000 from auth-static.conf.ext (the Maildir owner), and the stock
# dovecot/dovecot image has no "vmail" group to resolve.
